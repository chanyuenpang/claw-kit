{
  "id": "release-claw-kit",
  "version": "0.2.1",
  "title": "release-claw-kit",
  "status": "process.active",
  "goal": {
    "text": "Publish a verified claw-kit release, then refresh and verify the maintainer Codex installation from the published sources."
  },
  "requirements": {
    "summary": "Classify local changes to choose the correct release mode (CLI, Codex plugin, or other adapter), prepare the aligned release source, pass the guarded release gates, verify GitHub, npm, and the committed plugin payload, then run the separate published-source update phase inside the same plan.",
    "openQuestions": [],
    "acceptanceCriteria": [
      "Every local change is classified and the correct release mode (CLI, Codex plugin, or adapter-only) is chosen before touching versions.",
      "CLI + core use 3-segment versions; adapters use 4-segment CLI_VERSION.PATCH versions and reset to .0 on CLI bumps.",
      "The version, dependency pins, lockfile, plugin manifest, templates, materialized shared skills, and changelog form one reviewed release baseline.",
      "The guarded verifier passes before any tag is pushed and before @veewo/claw-core and @veewo/claw are published (CLI release only).",
      "GitHub source and tag, npm metadata and retrieval, the committed Codex marketplace payload, and a clean worktree are verified for the target version.",
      "Only after release completion, the global CLI and official claw-kit@claw-kit Codex installation are refreshed from published sources and verified exactly."
    ]
  },
  "tasks": [
    {
      "id": 1,
      "title": "Establish the release target and mode",
      "detail": "Read repository AGENTS.md, DISTRIBUTION.md, package manifests, the release ADR, current git status/branch/remotes, origin/main, tags, and npm latest versions. Classify every local change: does it touch core/CLI, codex-adapter, cindy-adapter, openclaw-adapter, or opencode-adapter? Choose CLI release if core or CLI changed; choose Codex plugin release if only codex-adapter changed; choose adapter-only release if only cindy/openclaw/opencode changed. Default to next CLI patch for the highest-impact change. Do not stash, discard user work, create a branch, or publish from an unexamined worktree.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "The target release mode, version, branch baseline, and every local change are classified.",
            "nextsteps": [
              "Prepare the release source according to the chosen mode.",
              "CLI release resets all adapter 4th segments to .0; adapter-only releases only bump that adapter.",
              "Keep disposable output out and preserve intentional user work."
            ],
            "nextTaskId": 2
          }
        }
      }
    },
    {
      "id": 2,
      "title": "Prepare the aligned release source",
      "detail": "Update version files according to the chosen release mode. For CLI: root, core, CLI (3-segment), reset adapters to <CLI_VERSION>.0 (4-segment), and set codex plugin manifest to match codex-adapter version. For adapter-only: bump only that adapter's 4th segment and its plugin manifest. Pin @veewo/claw-core in CLI and OpenClaw adapter. Update CHANGELOG.md. Regenerate package-lock metadata. Run npm run sync:template-versions before npm run sync:shared-skills, then npm run check:template-versions. Review all generated adapter copies and the complete diff.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "The target release surface is aligned and generated copies are reviewed.",
            "nextsteps": [
              "Choose verification proportional to the concrete release diff.",
              "Do not advance with stale template versions or unsynchronized shared skills."
            ],
            "nextTaskId": 3
          }
        }
      }
    },
    {
      "id": 3,
      "title": "Verify the release candidate proportionally",
      "detail": "Run git diff --check and the focused checks justified by the changed surfaces. Run npm run check:template-versions and the shared-skill synchronization test whenever templates or shared skills are involved. Run npm run check and targeted plugin bundle tests when their protected contracts changed. Use npm pack --dry-run for both publishable packages when package contents or dependency metadata are at risk. Expand to npm test only when the realistic regression risk justifies it. Fix failures and rerun the affected checks; record exact commands and results.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "The release candidate passed a risk-proportionate pre-push verification set.",
            "nextsteps": [
              "Commit the complete reviewed release baseline directly on main.",
              "Push it before invoking the guarded release verifier."
            ],
            "nextTaskId": 4
          }
        }
      }
    },
    {
      "id": 4,
      "title": "Commit, push, and confirm the release gate",
      "detail": "Commit the complete release baseline directly on main. Push it immediately to origin/main. Run git status --porcelain (must be empty), git branch --show-current (must be main), and confirm git rev-parse HEAD equals git rev-parse origin/main. Stop and classify any unexpected change or mismatch before continuing. The guarded verifier is the last chance to catch stale template or shared-skill drift before tagging.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "The release commit is pushed, main is exactly origin/main, and the worktree is clean.",
            "nextsteps": [
              "Run the guarded verifier, then tag and publish.",
              "Do not skip the verifier for adapter-only releases."
            ],
            "nextTaskId": 5
          }
        }
      }
    },
    {
      "id": 5,
      "title": "Tag and publish the release",
      "detail": "Run npm run verify:release. After it passes, create and push the correct release tag: v<version> for CLI, vcodex-<version> for Codex, vcindy-<version> for Cindy, vopenclaw-<version> for OpenClaw, vopencode-<version> for OpenCode. In CLI mode, run npm run publish:release to publish @veewo/claw-core then @veewo/claw. Create the GitHub Release from the immutable tag with concise notes. Do not attach a plugin ZIP; the committed marketplace snapshot at the Git ref is the Codex artifact.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "The release is tagged and published to npm (CLI) and GitHub.",
            "nextsteps": [
              "Prove GitHub, npm, committed plugin payload, and clean-worktree release evidence.",
              "Do not begin local update until registry propagation is sufficient for retrieval."
            ],
            "nextTaskId": 6
          }
        }
      }
    },
    {
      "id": 6,
      "title": "Verify the released artifacts",
      "detail": "Verify origin/main and the pushed release tag resolve to the release commit. For CLI releases, verify both npm packages expose the target version and dist-tags.latest; inspect CLI bin/dependencies and dist.tarball/dist.integrity/dist.shasum; perform an actual npm pack or isolated retrieval/install smoke. Verify the committed Codex marketplace plugin manifest and required skill payload at the same Git commit. Confirm the repository remains clean and local main still equals origin/main. Treat registry propagation delay as pending, not rollback; use the recovery guidance before retrying stale local retrieval.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "The release is complete across GitHub, npm (if CLI), the committed Codex payload, and the local repository gate.",
            "nextsteps": [
              "Begin the independent update boundary using only the published CLI and official GitHub marketplace.",
              "Keep release evidence distinct from maintainer-machine installation evidence."
            ],
            "nextTaskId": 7
          }
        }
      }
    },
    {
      "id": 7,
      "title": "Refresh the maintainer Codex installation",
      "detail": "After the release is fully verified, apply the claw-kit:update workflow as an independently owned update stage. Refresh the global CLI from the published npm package first, then upgrade/install the official chanyuenpang/claw-kit GitHub marketplace source and enable only claw-kit@claw-kit. Disable any claw-kit@claw-kit-local identity. Do not install the CLI or plugin from unpublished workspace files, even though this plan is running inside the repository.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "Published-source CLI and official Codex plugin refresh commands completed.",
            "nextsteps": [
              "Verify the runtime, marketplace identity, source manifest, and active cache exactly.",
              "Restart Codex and use a new task for loaded-skill verification."
            ],
            "nextTaskId": 8
          }
        }
      }
    },
    {
      "id": 8,
      "title": "Verify installation and close out",
      "detail": "Verify the Windows claw shim path, claw --version, npm list -g @veewo/claw --depth=0, and a repo-local claw context smoke that proves current flat project config is not rewritten to legacy nested fields. Verify the official marketplace origin, enabled claw-kit@claw-kit identity, source and active cache plugin versions, matching manifest hashes, and expected public skill payload. Confirm release-claw-kit remains repository-local and the local plugin identity is disabled/absent. Restart Codex and start a new task before claiming the newly loaded skill version. Report the target release mode, versions, release commit/tag, exact GitHub/npm/plugin evidence, checks run, and any unverified restart-dependent state.",
      "status": "pending"
    }
  ],
  "references": [
    {
      "path": "references/release-protocol.md",
      "why": "Provides exact command groups, version scheme, release modes, and failure recovery for the claw-kit release."
    },
    {
      "path": "CONTENT-COVERAGE.md",
      "why": "Maps the existing release and update contracts to this template-backed skill."
    }
  ],
  "rules": [
    "Follow returned workflowGuidance before advancing.",
    "Honor repository AGENTS.md and preserve user-owned changes; never use stash or destructive cleanup to manufacture a clean release gate.",
    "Classify local changes to choose the correct release mode: CLI, Codex plugin, or adapter-only.",
    "CLI + core use 3-segment versions; adapters use 4-segment CLI_VERSION.PATCH and reset to .0 on CLI bumps.",
    "Codex plugin manifest version equals codex-adapter package version (no timestamp).",
    "Release preparation may mutate source; npm run verify:release and npm run publish:release must remain side-effect free except for the intended registry publish.",
    "Do not publish until the release commit is pushed and clean local main exactly equals origin/main.",
    "Publish @veewo/claw-core before @veewo/claw and never bypass the guarded entrypoints.",
    "The artifact release and the maintainer installation refresh are separate completion boundaries even though this template sequences both.",
    "Never install from unpublished workspace content during the update phase; use the published npm package and official GitHub marketplace.",
    "Do not claim completion until both release evidence and installation evidence pass, except for an explicitly narrowed release mode."
  ]
}
