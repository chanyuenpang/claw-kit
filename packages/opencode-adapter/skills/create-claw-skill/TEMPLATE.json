{
  "id": "create-claw-skill",
  "configOverride": {
    "truthDispatch": "final_only"
  },
  "title": "create-claw-skill",
  "status": "process.active",
  "goal": {
    "text": "Convert a specified text skill or user idea into a claw-template-backed skill in the same skill package, with an adjacent fallback document."
  },
  "requirements": {
    "summary": "Translate an existing text skill or user idea into a claw skill in the target skill package. The visible entry should route through `claw plan create --template <template-id>`, the template should own workflow guidance, and the original or distilled skill text should remain available as an adjacent fallback document.",
    "openQuestions": [],
    "acceptanceCriteria": [
      "The specified skill package's visible entry clearly routes to `claw plan create --template <template-id>`.",
      "The resulting template uses plan-like task skeletons plus `guidance.onDone`.",
      "Any route-heavy behavior is restored through template guidance and explicit `choiceId`.",
      "The original or distilled skill text is preserved as an adjacent fallback document in the same skill package.",
      "The converted package preserves source behavior across the claw entry, template, references, and fallback document.",
      "Important source knowledge that is too verbose for the template is distilled into compiled knowledge references inside the converted skill package and listed in template.references.",
      "The generated claw entry includes the standard entry routing section for no-`.claw` fallback, direct, execution-time subplan, and batch-or-mixed root-plan usage.",
      "Batch or mixed callers split work into execution-time subplans instead of replacing the skill workflow with one broad conversion script.",
      "The generated entry only advertises `claw plan create --template <template-id>` when that template will be resolvable from the skill package or another supported runtime template registry.",
      "The generated template passes `claw template validate --template <template-id>` before completion.",
      "A content coverage check maps important source skill information to its converted home before completion."
    ]
  },
  "tasks": [
    {
      "id": 1,
      "title": "Choose the wrapping route immediately after entry",
      "detail": "Make a quick route choice based on whether the source is an existing skill or mainly a user idea, record that choice, and continue into the selected wrapping path.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "choices": {
            "simple": {
              "mergeMode": "override",
              "summary": "Route chosen: continue with the lightweight wrapping path.",
              "nextsteps": [
                "Keep the new template compact and mostly linear.",
                "Convert the specified skill entry in place and keep the source text as the adjacent fallback document."
              ],
              "nextTaskId": 2,
              "delegateTruth": false
            },
            "routing": {
              "mergeMode": "override",
              "summary": "Route chosen: continue with the route-aware wrapping path.",
              "nextsteps": [
                "Add explicit `guidance.onDone` routes to the generated template where the workflow really branches.",
                "Convert the specified skill entry in place and keep the source text as the adjacent fallback document."
              ],
              "nextTaskId": 2,
              "delegateTruth": false
            },
            "idea-first": {
              "mergeMode": "override",
              "summary": "Route chosen: distill the idea first, then wrap it as a claw skill.",
              "nextsteps": [
                "First distill the user idea into a concise fallback document in the target skill package.",
                "Then create the claw entry and template around that fallback."
              ],
              "nextTaskId": 2,
              "delegateTruth": false
            }
          }
        }
      }
    },
    {
      "id": 2,
      "title": "Draft the template-owned workflow skeleton",
      "detail": "Use the stub generator as the preferred starting point when creating or replacing a target skill package: `node <create-claw-skill-dir>/scripts/create-claw-skill-stub.mjs --skill-name <skill-name> --template-id <template-id> --target-work \"<target-work>\" --fallback-doc <fallback-doc> --out <target-skill-package>`. The scaffold writes the workflow template as skill-local `TEMPLATE.json`; keep the template id inside that file aligned with the generated entry route. Then fill in the generated TODOs from the source skill. The generated scaffold is not completed work: revise the plan-like template skeleton, decide which tasks need `guidance.onDone.default` or `guidance.onDone.choices`, preserve execution-critical source behavior in template guidance or rules, and identify source knowledge that should become compiled knowledge references inside the converted skill package instead of inline template prose.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
              "summary": "The template skeleton is ready; continue with the user-facing skill surfaces.",
              "nextsteps": [
              "If a new scaffold is useful, start from `scripts/create-claw-skill-stub.mjs` rather than hand-writing the entry/template/fallback shape from scratch.",
              "Wire the target skill entry to `claw plan create --template <template-id>`.",
              "Keep the original or distilled skill text readable as the adjacent fallback document.",
              "Create compiled knowledge references inside the converted skill package for important source knowledge that is too verbose for the template."
            ],
            "nextTaskId": 3,
            "delegateTruth": false
          }
        }
      }
    },
    {
      "id": 3,
      "title": "Convert the target skill entry and preserve its fallback document",
      "detail": "Update the specified skill package so its visible entry is a thin claw-facing entry, preserve or derive the fallback document beside it, create any needed compiled knowledge references inside that same skill package, include the standard entry routing section with the no-`.claw` fallback branch, and keep bundled helper files or references usable from that package. Keep generated `SKILL.md` compact: execution rules, tool usage, verification gates, content coverage, and detailed workflow control should live in the generated template, guidance, rules, references, or fallback rather than in the visible entry.",
      "status": "pending",
      "guidance": {
        "onDone": {
          "default": {
            "mergeMode": "override",
            "summary": "Skill surfaces are in place; finish by verifying the routing contract.",
            "nextsteps": [
              "Verify that the target skill entry points at the intended template id.",
              "Verify that the target skill entry explains no-`.claw` fallback, direct plan entry, execution-time subplan entry, and root-plan orchestration for batch or mixed requests.",
              "Verify that the adjacent fallback document still reflects the original skill behavior.",
              "Verify that compiled knowledge references live in or beside the converted skill package and are listed in the generated template references with clear reasons.",
              "Verify that required source-tool usage is represented in the generated template, not only in the visible `SKILL.md`.",
              "Run `claw template validate --template <template-id>` against the generated template.",
              "Then run a content coverage check that maps source information to entry, template, references, compiled references, or fallback."
            ],
            "nextTaskId": 4,
            "delegateTruth": false
          }
        }
      }
    },
    {
      "id": 4,
      "title": "Validate template and run the content coverage check",
      "detail": "First run `claw template validate --template <template-id>` and check that route-aware tasks require `choiceId` when defined. Then run a content coverage check: compare the source skill against the converted package and map important source information to its converted home (`entry`, `template.tasks`, `template.guidance`, `template.rules`, `template.references`, `compiled reference`, or `fallback`). Treat tool usage as template-owned execution information unless it is only a trigger/routing hint. Cover trigger rules, tools, workflow steps, branches, constraints, verification gates, examples, scripts, links, and compiled knowledge references. If any important source detail is omitted, record why.",
      "status": "pending"
    }
  ],
  "references": [
    {
      "path": "docs/template-authoring-guide.md",
      "why": "Canonical guide for the plan-like template format, guidance.onDone, choiceId, configOverride, and template validation."
    },
    {
      "path": "docs/create-claw-skill-lessons.md",
      "why": "Design lessons for keeping create-claw-skill lightweight while moving explanation and rationale into referenced docs."
    }
  ],
  "rules": [
    "Keep task ids numeric.",
    "Keep workflow control in the template.",
    "Let `plan.configOverride` come only from the template.",
    "Convert the specified skill entry in place and preserve the original text as an adjacent fallback document.",
    "Create compiled knowledge references inside the converted skill package for important source knowledge that is too verbose for the template.",
    "Use `scripts/create-claw-skill-stub.mjs` as the preferred starting scaffold for generated claw skill surfaces; it writes the generated workflow template as skill-local `TEMPLATE.json`.",
    "For generated claw skills, prefer skill-local `TEMPLATE.json` over project skill-local `TEMPLATE.json` so the skill package carries its own workflow.",
    "Keep generated `SKILL.md` entries thin; move execution rules, tool usage, workflow control, validation gates, and content coverage requirements into the generated claw template or its references.",
    "Required tool usage from the source skill must be represented in the generated template, guidance, rules, references, or compiled knowledge; do not leave it only in the visible skill entry.",
    "Run a content coverage check after template validation and before claiming the conversion complete.",
    "For batch or mixed requests, split work into one root-plan task per target skill or coherent skill-shaped unit and create `create-claw-skill` subplans at task execution time.",
    "Keep batch routes orchestration-oriented: the root plan may define target ordering, naming conventions, shared output boundaries, and batch-level risks.",
    "Batch root task titles should look like `Run a create-claw-skill subplan, convert <skill-name>`.",
    "Batch root task details should begin with a goal like `Goal: run a create-claw-skill subplan to convert <source-skill-path> into a claw skill.`",
    "Batch root task details should state that the subtask is satisfied by creating and completing that target subplan, not by running the claw skill workflow in the root plan.",
    "Batch root task details should require `claw subplan create --parent <root-task-name> --task-id <id> --template create-claw-skill` before editing the target.",
    "For subplan-owned subtasks, the root plan records the subplan result and marks the parent task done after incorporating that result.",
    "The root plan should not perform per-target source analysis, file edits, template validation, or content coverage checks; each target subplan owns those actions.",
    "Reusable helpers are allowed as implementation aids, but they must not replace each target's subplan, validation, and content coverage check.",
    "Do not replace execution-time subplans with one broad conversion script unless the user explicitly asks to bypass the claw skill workflow.",
    "Every generated claw skill entry should include the standard batch or mixed request route block; substitute skill name, template id, and target-work wording instead of redesigning batch routing per skill.",
    "Generated skill batch task titles should use the generic shape `Run a <generated-skill-name> subplan, complete <target-work>`.",
    "Generated skill batch task details should say the task is satisfied by creating and completing that target subplan, then recording the subplan result in the root plan.",
    "Do not leave a converted global or plugin skill pointing at a template that exists only in the development repository.",
    "Generated claw entry skills include the standard entry routing section.",
    "Generated project claw entry skills route to the adjacent fallback document when the current workspace has no `.claw` directory.",
    "Validate generated templates with `claw template validate --template <template-id>`."
  ],
  "keyDecisions": [],
  "retrospective": {
    "summary": ""
  }
}
