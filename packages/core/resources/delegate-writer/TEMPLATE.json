{
  "id": "internal-knowledge-delegate",
  "version": "0.2.1",
  "scope": "session",
  "title": "internal knowledge delegate",
  "status": "process.active",
  "goal": {
    "text": "Complete one dispatched knowledge finalization without interaction or recursive delegation."
  },
  "requirements": {
    "summary": "Wait for Stop capture, claim the immutable job, execute its generated assignment subplan sequentially, and persist one terminal result.",
    "openQuestions": [],
    "acceptanceCriteria": [
      "The job is located by the supplied project root and finalization id.",
      "Only a successful claim can execute assignments.",
      "Every assignment runs sequentially in the generated session-scoped subplan.",
      "A successful claim always reaches exactly one terminal knowledge done call."
    ]
  },
  "tasks": [
    {
      "id": 1,
      "title": "Wait for Stop capture",
      "detail": "Run `claw knowledge wait --project-root <project-root> --finalize-id <finalize-id>` using the exact values supplied by dispatch. Allow at least 310 seconds for this command because it may wait up to 300 seconds for the parent Stop hook. This wait is only for job readiness; do not create or inspect a binding. Retain the returned jobPath.",
      "status": "pending"
    },
    {
      "id": 2,
      "title": "Claim the knowledge job",
      "detail": "Run `claw knowledge claim --job <jobPath>`. Stop without executing assignments if claimed is false. Retain claimToken, templatePath, assignments, and immutable writer configuration. Do not execute any assignment before a successful claim.",
      "status": "pending"
    },
    {
      "id": 3,
      "title": "Execute the assignment subplan",
      "detail": "Run `claw subplan create --parent \"internal knowledge delegate\" --task-id 3 --template-file <templatePath>`, then follow the returned workflowGuidance. Execute each assignment yourself in order. Do not invoke a background finalizer, another writer agent, or a collaboration subagent.",
      "status": "pending"
    },
    {
      "id": 4,
      "title": "Persist the terminal result",
      "detail": "After claim, call `claw knowledge done --job <jobPath> --claim-token <claimToken>` exactly once. Use `--status succeeded --result <concise-result>` only when every assignment succeeded; otherwise use `--status failed --error <concise-error>`. Treat this as a finally-style obligation and then complete this delegate plan.",
      "status": "pending"
    }
  ],
  "references": [],
  "rules": [
    "Work unattended and never request user input, confirmation, or review.",
    "Do not modify the supplied plan or report materials.",
    "Do not switch between background and subagent execution policies.",
    "Do not continue to later assignments after one fails."
  ],
  "keyDecisions": [],
  "retrospective": {
    "summary": ""
  }
}
