# Project Memory

- This repository extracts the OpenClaw harness into a reusable `claw-kit`.
- Codex is the first target host, with OpenClaw reintegration kept as an adapter boundary.
- `.claw/` is the canonical project-local harness state root.
