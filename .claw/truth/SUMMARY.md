# Claw Kit Truth Summary

- `claw-kit` is a host-neutral `.claw` harness core extracted from OpenClaw semantics.
- Codex startup uses prompt-driven `using-claw-kit` session entry plus unified `SessionStart` startup-recovery context injection.
- `SessionStart` attempts session-bound active workflow recovery from current `.claw` state before falling back to default startup behavior.
- Recovered startup context injects the current plan content alongside the minimal claw workflow snapshot, and recomputed `workflowGuidance` remains the only next-step contract.
- `process.allTasksDone` is the root-plan ADR dispatch boundary; `end.completed` is archival closeout only.
- Release verification for this workflow split is anchored by `packages/core/test/core.test.ts` and `packages/cli/test/cli.test.ts`, especially the `plan write binds owner session key and SessionStart recovers active workflow snapshot` case.
- `0.1.52` release closeout completed the same publish/install loop with the 0.1.51 rebase already in place: workspace/package/plugin metadata moved to `0.1.52`, `packages/codex-adapter/.codex-plugin/plugin.json` reached `0.1.52+codex.20260624215321`, `npm test`, `npm run check`, and the core/CLI tarball dry-runs passed, both npm packages were published and verified at `0.1.52`, the local CLI refreshed to `C:\nvm4w\nodejs\claw.ps1` at `0.1.52`, and the local Codex plugin cache matched the same manifest version.
- `planning` is now the single visible planning skill; it absorbs lifecycle and `workflowGuidance` consumption rules that had previously been split across standalone Codex workflow skills.
- `using-claw-kit` now makes reading `planning` the first visible action, and no longer exposes separate bootstrap, claw-context, or reference-loading steps ahead of the main workflow.
- `create-claw-skill` is now the shared conversion entry that keeps the maintained shared-skill set intentionally small: `shared/skills/planning/SKILL.md`, `shared/skills/config/SKILL.md`, and `shared/skills/create-claw-skill/SKILL.md` are the canonical sources, while generated experiment-only trees such as `brainstorming` and `systematic-debugging` stay out of the formal shared corpus unless they are explicitly promoted later.
- `scripts/sync-shared-skills.mjs` now syncs only the maintained built-in shared skills under a repo-scoped lock so helper assets stay aligned across Codex and OpenCode adapters without accidentally promoting test-only skill trees.
- `create-claw-skill` 的默认输出形状已经收敛为原地转换指定 skill package：既有 skill 的 visible entry 会被 claw entry 替换，原始 skill 文本保留为同包内 adjacent fallback document，这样可以批量转换已安装文本技能而不额外生成第二套 visible skill 集合。
- `using-superpowers` 的转换已经固定为 lightweight wrapper + adjacent fallback 的保真形状：`packages/codex-adapter/skills/using-superpowers/SKILL.md` 只负责可见入口路由，`SUPERPOWERS-FALLBACK.md` 保存原始正文，而 `agents/openai.yaml` 和 `references/{codex-tools,copilot-tools,gemini-tools}.md` 作为同包复制资产继续维持源包语义；对应模板走同包 `packages/codex-adapter/skills/using-superpowers/TEMPLATE.json`，开发仓库里用 `node .\\packages\\cli\\dist\\bin.js template validate --file <path>` 校验，运行时随 `skills` payload 进入插件缓存并由 skill-local template loader 解析。
- canonical ADR: `using-superpowers-wrapper-entry-and-runtime-synced-template.md`
- `verification-before-completion` 的转换已经固定为 compact linear wrapper + adjacent fallback 的保真形状：`packages/codex-adapter/skills/verification-before-completion/SKILL.md` 只负责可见入口路由，`SUPERPOWERS-FALLBACK.md` 保存原始正文，而 `agents/openai.yaml` 继续维持源包语义；对应模板走同包 `packages/codex-adapter/skills/verification-before-completion/TEMPLATE.json`，开发仓库里用 `claw template validate --template superpowers-verification-before-completion` 校验，运行时随 `skills` payload 进入插件缓存并由 skill-local template loader 解析。
- canonical ADR: `verification-before-completion-compact-wrapper-and-runtime-synced-template.md`
- `verification-before-completion` 的转换也收敛成 compact linear wrapper + 轻量 five-step gate：`packages/codex-adapter/skills/verification-before-completion/SKILL.md` 只保留触发与路由，五步 gate 在同包 `TEMPLATE.json` 和 `CLAW-KNOWLEDGE.md` 中压缩表达，而 `SUPERPOWERS-FALLBACK.md` 继续承载完整 enforcement prose 与示例；`SUPERPOWERS-FALLBACK.md` 和 source `SKILL.md` 已通过 hash 比对确认一致，`agents/openai.yaml` 也是同包源文件的字节级拷贝，模板校验走 `node .\\packages\\cli\\dist\\bin.js template validate --file packages\\codex-adapter\\skills\\verification-before-completion\\TEMPLATE.json`。
- `writing-plans` 的转换必须保持 route-aware，而不是纯线性 wrapper：源技能包含 subagent-driven 和 inline execution 之间的执行交付分支，所以 `packages/codex-adapter/skills/writing-plans/SKILL.md` 只负责轻量入口路由，`CLAW-KNOWLEDGE.md` 记录 major section anchors / command examples，`SUPERPOWERS-FALLBACK.md` 保留完整 planning header、task structure、self-review 和 execution handoff prose；`SUPERPOWERS-FALLBACK.md` 与 source `SKILL.md`、`agents/openai.yaml` 的字节级复制关系已通过 hash 比对确认，而 `plan-document-reviewer-prompt.md` 也被保留在 converted package 中。
- canonical ADR: `writing-plans-route-aware-wrapper-and-review-prompt.md`
- 整个 superpowers 14 技能重做回合已经验证出稳定收敛形状：14/14 目标都通过各自的 `create-claw-skill` subplan 推进，开发期模板验证可走 `template validate --file ...`，Codex plugin bundle / install 链路把同包 `TEMPLATE.json` 随 `skills` payload 带入插件缓存，而最终 batch verification 对每个技能都确认了 required files、fallback 保真、sidecar 复制以及 `codex-adapter` check/build 通过。
- `create-claw-skill` 的生成型 claw entry 现在有稳定入口路由标准：进入 template 前要先把请求归类为 direct single-target、active parent-plan task 或 batch/mixed root orchestration；单目标走 `claw plan create --template <template-id>`，父计划任务在执行到达时走 `claw subplan create --parent <parent-task-name> --task-id <id> --template <template-id>`，batch/mixed 则先起 root plan 再在 task execution 时实例化 template subplan。
- `brainstorming` 经过真实 `create-claw-skill` 流程重做后，`packages/codex-adapter/skills/brainstorming/SKILL.md` 里的可见模板必须显式承载 `decomposition choice`、`design approval`、`written-spec review` 和 `writing-plans` handoff 等控制流门槛；`visual-companion.md`、`spec-document-reviewer-prompt.md` 以及 package runtime assets 继续作为一等引用保留，而不是被降级成可省略的附属物。
- 批量把 14 个 Codex superpowers plugin skills 转成 claw skills 时，应由根计划为每个目标技能各自发起 `create-claw-skill` 子计划；转换目标默认落在 `packages/codex-adapter/skills/<skill-name>/`，生成模板 id 需要带前缀以避免与 canonical 模板碰撞，且生成树保留 adjacent fallback/helper folders 但不自动纳入 shared canonical sync set，除非显式晋升。
- canonical ADR: `superpowers-batch-conversion-uses-per-skill-create-claw-skill-subplans.md`
- 重做后的批量转换不再接受之前那批共享生成器产物作为完成态；每个 superpowers skill 都必须在自己的 `create-claw-skill` subplan 里完成 source analysis、template design、fallback preservation、compiled knowledge 和 content coverage，root plan 只负责 orchestration。
- `using-claw-kit` 的 visible entry 现在同时拥有复杂度门禁：先恢复已有 `.claw` workflowGuidance / active task；没有 task scope 时先按入口 skill 内的复杂度评分判定是否进入正式 claw workflow，只有需要正式 workflow 的请求才会继续到 `claw plan create`。低分轻量请求在入口处直接绕过 `claw search`、`claw plan create` 和 `workflowGuidance`，`direct` 仍只是隐藏兼容命令而不是公开主流程。
- `create-claw-skill` 现在也被硬化成 template-first 的显式入口：只要没有同一 workflow 的 recovered task state，首个动作就必须是 `claw plan create --template create-claw-skill --title "<source-skill-or-target-skill-name>"`，连 skill 自测也不能先用 pre-template analysis 或 temp validation 把真实 template flow 绕过去；这一类显式模板 skill 也不该被通用 `using-claw-kit` complexity gate 抢先分流。
- `create-claw-skill` 对已有 skill 的默认转换形态是原地（in-place）改造：visible `SKILL.md` 替换成 claw entry，原始文本保留为同一 skill package 里的相邻 fallback 文档，避免批量转换时额外分裂出第二套可见 skill 集合；生成的 template 落在同一 skill package 的 `TEMPLATE.json`，并要用 `claw template validate --template <template-id>` 或 `claw template validate --file <skill>/TEMPLATE.json` 做最终校验。
- canonical ADR: `create-claw-skill-entry-route-and-fallback.md`
- Active adapter startup surfaces now use `startupRecovery` naming in `claw context` results, hook logging, and reference filenames such as `codex-startup-recovery.md`; the canonical SessionStart entry is the `claw hook SessionStart` CLI command.
- The non-recovered startup prompt is intentionally slim: it should not repeat project-root, protocol-check, or "report recovered state" lines.
- In the normal startup prompt, the current `@claw-kit` thread is explicitly pre-authorized for Goal mode and required delegated subagents including `truth-writer` and `adr-writer`, so missing per-turn authorization is not a valid blocker.
- `claw search` is the Codex-facing recall command; `memory.externalDocPaths` extends project recall sources.
- `claw search --query` stays compatible, and `claw search index --refresh` is the explicit project index refresh entrypoint that returns `search.index.refresh`.
- `claw search` is project-scoped document recall for project memory, truth, ADR, and external docs; code investigation still belongs to `researcher` plus GitNexus, not `claw search`, and the host should not pre-read the search skill inline before dispatching `researcher`.
- `claw search` only indexes configured external memory paths for `.md` files, and `claw search index --refresh` now materializes project-scoped vector data plus `vectorIndex` metadata from `memory.embedding`.
- template guidance routing now uses persisted `plan.templateId` plus template-scoped `plan.configOverride`; `guidance.onDone.default` and `guidance.onDone.choices` can both alter returned workflow guidance through `mergeMode: "override" | "replace"` and optional `delegateTruth: false`, while `guidance.onDone.choices` still drives route-aware `choiceId` validation for `claw task done` and `claw plan edit --task-choice`; template-only guidance stays out of agent-facing runtime task content.
- `packages/core/src/memory.ts` now normalizes SQLite busy/locked contention through `MEMORY_STORE_BUSY`, with `PRAGMA busy_timeout` on every memory connection and `PRAGMA journal_mode = WAL` on write/index-refresh connections, so parallel search and refresh are still allowed until the same `.claw/memory.sqlite` is actually locked.
- `0.1.50` release closeout is fully verified and pushed: registry metadata for `@veewo/claw-core` and `@veewo/claw` both resolved to `0.1.50`, `@veewo/claw` dependency metadata stayed pinned to `@veewo/claw-core: 0.1.50`, `npm run check` passed after repairing the BOM on `.claw/truth/adr/sqlite-memory-store-concurrency-and-busy-error-semantics.md`, `npm test` passed with core `85/85` and CLI `50/50`, both package tarball dry-runs previewed `0.1.50`, the local CLI was restored to `C:\nvm4w\nodejs\claw.ps1` at `0.1.50`, the Codex plugin cache was refreshed to `0.1.50+codex.20260623210218`, and `HEAD` / `origin/main` both ended at `d3bdb0dcf60440daafae3493a80f4fe377015a04`.
- 2026-06-24 release audit confirmed registry `latest` was still `0.1.50` at round start, so the next patch target became `0.1.51`; the release-surface bump again covered root `package.json`, `packages/core/package.json`, `packages/cli/package.json`, `packages/codex-adapter/package.json`, `packages/openclaw-adapter/package.json`, `packages/opencode-adapter/package.json`, `package-lock.json`, and `packages/codex-adapter/.codex-plugin/plugin.json`, with plugin manifest target `0.1.51+codex.20260624140756`.
- 0.1.51 release verification passed the full local gate: `npm test` passed with core `85/85` and CLI `50/50`, `npm run check` passed, `npm run test:codex-plugin` passed `5/5`, `npm run test:opencode-plugin` passed `5/5`, and both package tarball dry-runs produced `veewo-claw-core-0.1.51.tgz` and `veewo-claw-0.1.51.tgz`.
- 2026-06-24 的 `release-next-version-and-refresh-local-installs` 完成计划把本地工作区先 rebase 到 `0.1.51` baseline，再把 root、`packages/core`、`packages/cli`、`packages/openclaw-adapter`、`packages/codex-adapter`、`packages/opencode-adapter` 和 `package-lock.json` 一起推进到 `0.1.52`，并将 `packages/codex-adapter/.codex-plugin/plugin.json` 设为 `0.1.52+codex.20260624215321`。
- 这轮 `0.1.52` 发布前的本地闸门仍然是 `npm test`、`npm run check`、`npm pack --dry-run -w @veewo/claw-core` 和 `npm pack --dry-run -w @veewo/claw`；它们都通过后才依次发布 `@veewo/claw-core@0.1.52` 和 `@veewo/claw@0.1.52`，并在发布后把本地 CLI 刷新到 `claw --version = 0.1.52`，同时把 Codex plugin cache 刷新到 `C:\Users\chany\.codex\plugins\cache\claw-kit-local\claw-kit\0.1.52+codex.20260624215321`。
- The 2026-06-26 release round advanced the full workspace/package surface to `0.1.53` and the Codex plugin manifest to `0.1.53+codex.20260626141302`, again covering root `package.json`, `packages/core/package.json`, `packages/cli/package.json`, `packages/codex-adapter/package.json`, `packages/openclaw-adapter/package.json`, `packages/opencode-adapter/package.json`, `package-lock.json`, and `packages/codex-adapter/.codex-plugin/plugin.json`.
- 0.1.53 release verification passed the full local gate: `npm test` passed with core `85/85` and CLI `50/50`, `npm run check` passed, `npm run test:codex-plugin` passed `5/5`, `npm run test:opencode-plugin` passed `5/5`, and both package tarball dry-runs produced `veewo-claw-core-0.1.53.tgz` and `veewo-claw-0.1.53.tgz`.
- `@veewo/claw-core@0.1.53` and `@veewo/claw@0.1.53` are now published with `version = latest = 0.1.53`; `npm view @veewo/claw bin --json` still resolves `bin.claw = "dist/bin.js"`, and the local Codex plugin cache was refreshed to `C:\Users\chany\.codex\plugins\cache\claw-kit-local\claw-kit\0.1.53+codex.20260626141302`, where the installed `.codex-plugin/plugin.json` matches the new low-complexity skip-before-plan entry copy.
- GitHub-docs round 的可恢复性来自 session rollout JSONL 和 archived task plan：恢复时应先抽取证据、再 replay 精确 patch、最后验证内容；root `README.md`、`docs/project-json-reference.md`、package README 和 package metadata 各自承担不同公开面职责，`npm pkg get` 是核对 `@veewo/claw` 与 `@veewo/claw-core` 元数据的实用验证。
- GitHub-facing docs 和 package metadata 的恢复协议已经固定：`git restore` / `git clean` 后优先从本地 rollout 和 completed plan replay 精确 patch，再做最小重建；公开文档继续按 root README、`docs/project-json-reference.md`、package README 和 package metadata 分层，repo-relative links 不能改成绝对路径。
- `README.md` 现在把读者显式导向 `docs/index.html` 作为 GitHub Pages 产品页入口，而 `docs/index.html` 下面的零构建产品介绍页本身已经形成可复用壳：双语内容模型按语言分组但共享 section id，页面脚本把纯 markup builder 和浏览器 mount 路径拆开，`scripts/product-deck.test.mjs` 负责回归双语 parity、HTML escaping 和 section 渲染计数，适合后续同类 scroll-driven bilingual product deck 直接复用。
- `docs/config-guide.html` 进一步确认了第二层 GitHub Pages 文档也应保持 `docs/` 下的零构建静态实现，且更适合按页面拆分 `content` / `render` / `style` 资产；首页 product deck 的最终 closing link 已稳定指向 `./config-guide.html`，并由 `scripts/product-deck.test.mjs` 与 `scripts/config-guide.test.mjs` 分别覆盖首页入口和二级页内容回归，而 `docs/project-json-reference.md` 继续承担更深的 canonical 字段细节层。
- 这套 `docs/index.html` 产品页壳的 hero 定位已进一步收敛：英文主标题固定为 `Claw Kit, a harness for complex projects and long-running tasks`，中文 hero 也同步围绕 `complex projects` 与 `long-running tasks` 叙事；左上角品牌链接改为直接指向 `https://github.com/chanyuenpang/claw-kit`，并由 `scripts/product-deck.test.mjs` 回归这些入口层事实。
- 这轮 `GitHub Pages 产品页视觉重构与产品页化优化` 说明了同一套 deck 的布局工作必须以浏览器截图审查为准，而不是只看 CSS；实际问题集中在 hero 右偏留白、problem section 断裂、continuity section 的装饰词竞争，以及 workflow section 只需做 headline 降权式精修，`docs/assets/product-deck.js` / `docs/assets/product-deck.css` 继续承担结构与表现，`docs/assets/product-deck-content.js` 继续保持跨语言内容对齐。
- 这轮 redesign 的关键可复用规则是：目标是 product-page shell，不是 documentation-like scroll page；浏览器 inspection 必须优先于 CSS-only reasoning；hero、problem、continuity、workflow 应各自承担不同 compositional role，而不是共享同一套 oversized left-title skeleton。
- 最新一轮 GitHub-facing root README 回合把公开定位写得更具体：`claw-kit` 只在已初始化 `.claw` 的仓库里表现为顺滑 workflow，不是全局接管层；planning 可启用、可禁用、也可路由到自定义 planning skill；并且 `claw-kit` 可以嵌入到另一种 harness / host 外壳里继续承担项目 workflow 角色，相关叙事落点仍然是 root README 的 intro、What it gives a project、Why teams use it、How it fits with other tools 四个段落。
- GitNexus 定位清理这轮把 public positioning 收窄为弱关联补充：最终 public positioning 围绕 `plan before execute`、与其他 harness / skills 协同、truth/ADR 的可复用可检索性，以及 canonical + personal config 的协作价值组织；`GitNexus` 仍只是深度代码调查与关系追踪的可选补充，这套叙事已经一致落到 `README.md`、`packages/cli/README.md` 和 `docs/project-json-reference.md`，root README 也不再承载逐步发布流程。
- Search-and-recall 文案收口进一步压缩了 public doc 面：root `README.md` 不再放长篇 search-and-recall 解释，`packages/cli/README.md` 只保留 `claw search` 的 retained-context boundary，而 adapter `references/project-config-reference.md` 只作为备份配置说明，不是 harness contract 或 required startup flow。
- `claw init` and `claw context` protocol repair now auto-fill a minimal default local embedding config into older `.claw/project.json` files using `Snowflake/snowflake-arctic-embed-m-v2.0`; default vector indexing stays runtime-enabled but no longer persists `store.vector.enabled = true` or a default `modelCacheDir` into project config, and `store.vector` is only retained for explicit `enabled: false` or `extensionPath`.
- local embedding cache resolution is now runtime-driven: explicit `modelCacheDir` wins only when that local cache already has the model; otherwise an existing platform-global cache is reused; if neither has the model then downloads go to the explicit local cache when configured, or to the platform-global cache by default; `.claw/models` remains the fallback when the global cache directory is unavailable.
- `packages/core/src/embedding-worker.ts` now resolves `@huggingface/transformers` through a project-first `resolveTransformersModule(projectRequire, workerRequire)` fallback, so `@veewo/claw-core` owns the runtime dependency graph while project-local installs can still override it.
- In the `claw-kit` repo, `.claw/project.json` no longer carries legacy `memory.embedding.local.modelCacheDir = ".claw/models"`, and the project-local `.claw/models` tree has been removed after the Snowflake models were folded into `C:\Users\chany\AppData\Local\claw\models`.
- `claw-kit` 0.1.40 已作为 patch release 发布；这轮 closeout 继续要求统一 workspace/package/plugin/changelog 版本、按 `@veewo/claw-core` 先于 `@veewo/claw` 的顺序发布、刷新本机全局 `claw` CLI 与本地 Codex plugin cache，并完成 end-to-end 本地 release 状态复核。
- 这次 0.1.40 release 进一步确认：release 目标版本要以 npm registry 当下已发布版本为准；如果 `@veewo/claw` 刚发布后的本机 CLI 刷新先撞到 registry 传播延迟，可在 `npm view @veewo/claw version` 已返回目标版本后重试一次 `npm run install:local-cli`，不把这类短暂失配误判成发布失败。
- 这轮 0.1.40 的收口证据是 registry、全局 `claw` CLI 与本地 Codex plugin cache 全部对齐到同一轮发布，其中 plugin manifest / cache 目标版本是 `0.1.40+codex.20260616130425`，缓存目录下 24/24 个同步文件 SHA256 与仓库副本一致。
- `sync-latest-remote-and-refresh-local-installs` 这条 closeout 协议现在明确要求：脏工作树先用包含未跟踪计划文件的具名 stash 完整保护，再 fast-forward/确认 `origin/main` 并恢复 stash；实际文本冲突只用 `apply_patch` 保留双方有效语义，且在无未合并路径、本地修改集合仍完整、`git diff --check` 通过前保留 stash 作为保险。之后再刷新全局 `claw` CLI、本地 Codex plugin cache，并做仓库 SHA、CLI 路径、缓存版本和逐文件 hash 的统一复核；在这台 Windows 机器上，`schannel` TLS 失败可用一次性的 `git -c http.sslbackend=openssl ...` 旁路验证，但不改全局 Git 配置。
- 这条 sync 协议的补充恢复事实是：如果 `github.com` 在这台 Windows 机器上被解析到 `127.0.0.1`，可以用一次性的 `git -c http.curloptResolve=github.com:443:140.82.112.4 ...` 局部覆盖 DNS 后完成 fetch / pull；这比改仓库状态、丢弃本地编辑或修改全局 Git 配置更安全。
- 这条 sync/install 协议还包括一个 Windows CLI 恢复边界：如果 `scripts/install-cli.ps1` 因超时在卸载旧全局包后、重新安装新版本前中断，机器会短暂失去 `claw` 命令；已验证的恢复路径是直接运行 `npm install -g @veewo/claw`，然后用 `claw --version`、`npm list -g @veewo/claw --depth=0` 和 `Get-Command claw` 复核。
- 本地 Codex plugin cache 的完成态也要双重验证：先确认 installer 输出的目标缓存路径和版本，再至少核对一个仓库文件与缓存副本的 SHA256 完全一致；单看 installer 成功输出不够。
- 在这台 Windows 机器上，如果默认 `git fetch origin` 报 `schannel: failed to receive handshake, SSL/TLS connection failed`，应先用 `Test-NetConnection` 验证 443 连通性，再用 `git -c http.sslbackend=openssl ls-remote origin refs/heads/main` 区分 `schannel` 后端问题和真实网络故障。
- 这次 release 把 `process.wait` / `process.discussing` / resumed active 语义稳定下来：暂停态要暂停 Goal Mode，恢复后通过 `process.active` 重新进入执行，并由 `process.resumedActive` / `on_resume_process_active` 接管恢复。
- `claw init` 现在会在项目根目录自动补齐 claw-kit 专用 `.gitignore` 规则块，但 `.gitignore` 变更只属于 `initProject()`；`project-check` / protocol repair 与 `claw context` 不会写 `.gitignore`，重复 init 也不会重复追加同一规则块。对应 canonical ADR 是 `init-project-gitignore-ownership`。
- runtime project resolution now deep-merges optional gitignored `.claw/project-override.json` over canonical `.claw/project.json`; override values may target any project field, and explicit `null` remains a real override rather than falling back to inherited config.
- canonical `.claw/project.json` uses flat project-level workflow toggles: `planning`, `externalPlanningSkill`, `goalMode`, `truthDispatch`, and `gitnexus`; legacy nested inputs such as `workflow.goalMode.enabled`, `workflow.truthDispatch.mode`, and `gitnexus.enabled` are accepted by protocol repair but normalized back to the flat canonical shape.
- `config` is now the dedicated claw-kit configuration skill: it must first distinguish shared team config (`.claw/project.json`) from personal local override config (`.claw/project-override.json`), and personal override examples must use the same flat canonical fields as project config.
- Local closeout/startup verification must compare the actual resolved global `claw` runtime files against the repo build, not just package versions; a same-version stale global `@veewo/claw-core` can make SessionStart rewrite `.claw/project.json` back to legacy nested config before repo-local checks flatten it again.
- Windows closeout archive pruning must not rely on `fs.rmSync(..., { recursive: true })`: non-ASCII archive task directories can fail or exit silently there, so `packages/core/src/task-retention.ts` now uses explicit recursive `unlinkSync` / `rmdirSync` removal with regression coverage for non-ASCII archive names in `packages/core/test/core.test.ts`.
- `claw init` 的默认 `.gitignore` 规则块现在继续保留 `!.claw/project.json` 作为 canonical 共享配置，同时默认忽略 `.claw/project-override.json` 这层本地运行时覆盖文件。
- `memory.embedding.local.device` is now an explicit project-level local-device selector, and `CLAW_EMBEDDING_LOCAL_DEVICE` / `CLAW_EMBEDDING_DEVICE` provide one-off rescue overrides; both feed `packages/core/src/embedding-local.ts`, which always keeps a `cpu` retry path for GPU-class `dml` / `cuda` devices.
- `claw search index --refresh` is now covered as a bounded 100-file batch process per run, with repeated refreshes automatically advancing the remaining backlog, and `packages/core/src/embedding-local.ts` is also covered for worker-side inference batching order preservation.
- `DEFAULT_LOCAL_EMBEDDING_BATCH_SIZE = 4` in `packages/core/src/embedding-local.ts` only controls internal local embedding slice size; `packages/core/src/memory.ts` keeps `DEFAULT_PROJECT_REFRESH_FILE_LIMIT = 100` as a separate per-refresh file cap, so the two knobs should be documented and tuned independently.
- 即使 `memory.embedding` 配置变化触发向量 reset，`claw search index --refresh` 仍保持每轮默认 100 文件的 bounded batching；reset 后的后续 refresh 继续补完剩余文件，而不是因为模型切换而一次性整库重建。
- `packages/core/src/embedding-local.ts` now advances local inference in fixed internal batches within a single worker/model session, and `packages/core/src/embedding-worker.ts` / `packages/core/src/memory.ts` now use temp-file handoff instead of giant stdout JSON for embedding results.
- `packages/core/src/memory.ts` 的 project refresh 走单事务同步，embedding 失败会整体回滚；`indexDocEmbeddings(...)` 先按 `chunkMarkdownContent(content)` 拆 chunk，再把每个 chunk 原文送进 embedding worker，而 `packages/core/src/embedding-local.ts` 只做 4 条一批的 extractor batching，不额外做 truncation / `max_length` 截断。
- `packages/core/src/embedding-worker.ts` now clamps local tokenizer `max_length` with `resolveLocalTokenizerMaxLength(...)` before feature extraction; for `Snowflake/snowflake-arctic-embed-m-v2.0`, the safe runtime cap is `8192` even though the tokenizer may report `model_max_length = 32768`.
- This clamp fixes the reproducible local ONNX failure behind the stalled refresh: the failing batch was `.claw/truth/SUMMARY.md` chunk 3 at `19339` tokens, which tripped `SkipLayerNormalization` in the Snowflake local model; after the fix, the same reproduction succeeds and `tiny-world` refresh advanced from `100` docs to `200` docs with `pendingFileCount = 217`.
- Windows `claw plan done` now keeps JSON output even when completion refresh is asynchronous: the CLI launches the refresh worker across an external process boundary instead of using a directly detached child from the main stdout-emitting process.
- completion refresh status files now move through explicit `queued` and `running` states before the final finished payload, so long local embedding cold starts stay observable instead of looking stuck at submission time.
- `packages/core/src/memory.ts` now enforces a 30-minute hard timeout for `embedding-worker.js` by default and reports timeout details in the failure payload, so a hung embedding child cannot hold the completion-refresh process forever without a visible error.
- Large refreshes keep the existing `claw search index --refresh` contract while advancing backlog in bounded batches and keeping vector payload transport off stdout.
- Large-project retest evidence from `NeonSpark` shows the final working path: `claw search index --refresh` completed with `indexedCount: 698` and `vectorIndex.chunkCount: 33737` after the batching and temp-file fixes.
- Chinese and multi-term retrieval quality now also uses shared keyword-term extraction plus document-level hybrid ranking signals, so exact Chinese hits can outrank weaker project-memory noise without expanding the public config surface or turning `claw search` into code search.
- In the `claw-kit` repo itself, `memory.externalDocPaths` stays empty, so local project recall excludes the repository `docs/` folder and indexes only `.claw` memory/truth/ADR Markdown.
- `claw search index --refresh` 现在把当前项目的 sqlite memory index 视为增量同步目标，而不是每次都全量删库重建。
- project memory sync 会记录 `docs.content_hash`；未变更的 markdown 文档会复用原有 sqlite `docs` row 与 embeddings，变更文档只重算并替换自身记录，被删除的 markdown 文档会从 `docs`、`docs_fts`、`doc_embeddings` 清理。
- 对于历史上仍保留 `docs` 行、但缺少 `doc_embeddings` 的旧数据，refresh 会在 `insertDocs` 之后继续扫描并回填缺失向量，避免 `vector_index.chunkCount` 因旧记录漏索引而停在 0。
- `memory.embedding` 配置变化会触发整套向量重建，保证向量数据与 `vectorIndex` / embedding metadata 保持一致。
- Project-level `claw search --query` now generates a real query embedding and runs a trimmed hybrid recall that fuses vector candidates with FTS results.
- project-level `claw search --query` 现在有显式 keyword search plan：多词查询会同时保留整句 multi-term query 和逐词 fallback query，再把这些 keyword candidates 与现有 vector recall 融合。
- 这套 planner 对中文多词查询同样生效；像 `搜打撤 哈基宝` 这样的 query 不再只依赖一次“同一条同时命中所有词”的 FTS `MATCH`。
- project search 的 candidate recall 进一步对齐了裁剪版 OpenClaw 设计：先并行收集 widened keyword candidates、document-signal candidates 和 vector candidates，再做 coverage-aware 统一 rerank。
- rerank 现在更强调 filename/path 命中与文档级 term coverage，并对 `contents.md`、`summary.md`、`index.md`、`README.md` 这类 index-like 文档做更强的降权，避免它们压过更聚焦的主题文档。
- Project-level search no longer silently falls back when vectors are missing; it now fails with `MEMORY_VECTOR_INDEX_REQUIRED` until a refreshed vector index exists.
- Task-scope memory search keeps the existing task-memory and FTS semantics; the hybrid/vector query path is project-only.
- `claw plan write`, `claw plan edit`, and `claw plan done` return compact `workflowGuidance` and `planSummary` contracts.
- `claw plan create` 的新生命周期已在 release flow 中验证：启用 planning 时会先落到 `process.discussing`，保留 seeded planning task 与 `Enter process.active` bridge task；追加 downstream tasks 不会覆盖 task 2，切到 `process.active` 后才返回 `goalTool.tool = create_goal`。
- `claw plan create` 的 seeded activation task 现在也会沿用同一个 goalMode objective：`goalMode = true` 且 host 不是显式 `opencode` 时，会把 recommended objective 追加到 activation task detail；Codex 默认 no-host 路径按 Codex-compatible 处理，`host: "opencode"` 保留旧的简洁 detail，`goalMode = false` 只保留 base detail。
- `claw plan write` now accepts `claw plan write "<title>" [--goal "<text>"]`; when `goal.text` is missing, requirements-stage guidance tells the agent to fill goal first, then fill the rest of the plan, and move to `process.active` as soon as requirements are clear.
- `goal.text` is the hard gate for leaving `prepare.requirements`; `process.active` cannot start without it.
- Thread goal mode no longer starts on `plan write`; `workflowGuidance.goalMode` is emitted on first entry into `process.active` with `allowOverwrite = true`, `setWhen = on_enter_process_active`, `ifNoActiveGoal = true`, `doNotOverwriteExisting = true`, and `supportedSurfaces = ["/goal", "create_goal"]`.
- Active `@claw-kit` threads are still pre-authorized for Goal mode and required delegated subagents, but that authorization only matters once the workflow actually returns those contracts.
- Truth and ADR deposition run through delegated writer specialists, not inline main-agent writes.
- Writer delegation contracts now carry explicit `skill` and `model` fields.
- `.claw/project.json` supports explicit `externalTruthSkill` and `externalAdrSkill` overrides with `null` defaults, and `memory.embedding` now carries the project embedding config plus index metadata support, including the default local `Snowflake/snowflake-arctic-embed-m-v2.0` / 768-dimension path plus explicit legacy `Snowflake/snowflake-arctic-embed-xs` / 384-dimension compatibility, both with Windows DirectML-to-CPU fallback.
- `packages/core/src/embedding-worker.ts` is the new embedding worker used for project embedding generation.
- This hybrid query migration follows the mature `openclaw-dev` memory query design, but only copies the smallest subset that fits `claw-kit`.
- 这次 multi-term recall 调整同样参考了 `openclaw-dev` 的 memory search 设计，但重点是 query planner、keyword recall 和 vector fusion，而不是单纯替换 embedding。
- Improve-search-candidate-recall-with-OpenClaw-reference 这一轮继续参考 `openclaw-dev` 的成熟 memory search，但只迁移适合 `claw-kit` 的 multi-route candidate recall、document-signal route 和 unified rerank 最小子集。
- incremental refresh 的 sqlite sync 语义参考了 `openclaw-dev` 的 bootstrap / refresh 思路，但在 `claw-kit` 里实现的是裁剪后的 sqlite 迁移与同步逻辑。
- README, CLI README, and core/cli tests already cover the new hybrid project search contract.
- README、`packages/cli/README.md` 与 `packages/core/test/core.test.ts` 已覆盖 incremental refresh 契约。
- `packages/core/test/core.test.ts` 已新增 query planner 语义和中文多词 project recall 场景覆盖；本轮校验通过 `npm test -- packages/core/test/core.test.ts` 与 `npm run check`。
- search candidate recall 这一轮的验证证据包括：`packages/core/test/core.test.ts` 50/50 通过、`npm run check` 通过，以及在 `NeonSpark` 的 live search 中，多词中文 query 不再让 `contents.md` 压过聚焦文档， conversational `搜打撤` 查询继续优先命中 system design 类文档。
- 当前 workspace 版本线是 `0.1.40`：`package.json`、`packages/core/package.json`、`packages/cli/package.json`、`packages/openclaw-adapter/package.json` 和 `packages/codex-adapter/package.json` 都已经对齐到 `0.1.40`，`packages/codex-adapter/.codex-plugin/plugin.json` 当前是 `0.1.40+codex.20260616130425`。
- release sync 现在有一条明确的预发布规则：如果本地已有 release guidance / truth 调整，先单独提交，再合并 `origin/main`；遇到 workflow guidance、CLI tests、Codex adapter docs/skills 或 `.claw/truth/` 冲突时，合并结果要同时保留远端更新和本地已验证的 goal gate / positional-title 行为，然后再跑 `npm test` 与 `npm run check`。
- 2026-06-23 的 project.json compatibility fixture run 用 temp copies 验证了 `claw-kit`、`NeonSpark`、`tiny-world`、`unity-mcp` 四个本地项目：new-shape configs 保持不变，legacy-shape configs 经 `claw check` 修复并经 `claw context` 解析成功，且默认 `memory.embedding.store.vector.enabled: true` 不会被重新写回。
- Release version bumps now include the full workspace/package/plugin surface: root `package.json`, `@veewo/claw-core`, `@veewo/claw`, Codex/OpenClaw/OpenCode adapter packages, CLI/OpenClaw dependency pins on `@veewo/claw-core`, `package-lock.json`, and the Codex plugin manifest `semver+codex.<timestamp>` version.
- 这次 release scope/version audit 把 registry 基线锁定在 `0.1.49`，并把下一次 patch target 选为 `0.1.50`；对应的版本元数据已同步覆盖 root `package.json`、`@veewo/claw-core`、`@veewo/claw`、`packages/codex-adapter`、`packages/openclaw-adapter`、`packages/opencode-adapter`、`package-lock.json` 和 `packages/codex-adapter/.codex-plugin/plugin.json`，其中 CLI 对 `@veewo/claw-core` 的依赖和 `openclaw` adapter 的依赖都已对齐到 `0.1.50`。
- `@veewo/claw-core@0.1.48` and `@veewo/claw@0.1.48` are now published with `latest = 0.1.48`; `@veewo/claw` registry metadata preserves `bin.claw = "dist/bin.js"` and dependency `@veewo/claw-core = 0.1.48`, so npm publish's bin normalization warning is not itself failure evidence.
- If npm metadata shows the new release but tarball retrieval still returns `ETARGET`, clear the local npm cache and retry tarball retrieval before treating the publish as broken; `npm cache clean --force` followed by `npm pack @veewo/claw@0.1.48` succeeded for this release.
- Local release closeout for `0.1.48` refreshed both runtime surfaces: global `claw` resolves to `C:\Users\chany\AppData\Roaming\npm\claw.ps1` with `claw --version = 0.1.48`, and the local Codex plugin cache at `C:\Users\chany\.codex\plugins\cache\claw-kit-local\claw-kit\0.1.48+codex.20260623165853` has matching manifest version plus the generated `config` skill.
- `0.1.49` patched the workflowGuidance `delegateSubagents` note to dispatch-time structured contract semantics: `When dispatching a subagent, each entry is a required structured contract whose fields must be honored directly.` This keeps workflowGuidance mandatory without implying that every returned optional truth-writer suggestion must always be dispatched.
- `@veewo/claw-core@0.1.49` and `@veewo/claw@0.1.49` are now published with `latest = 0.1.49`; the release source commit is `a593180` (`Release claw-kit 0.1.49`), global `claw --version` reports `0.1.49`, and the local Codex plugin cache is installed at `C:\Users\chany\.codex\plugins\cache\claw-kit-local\claw-kit\0.1.49+codex.20260623172440`.
- `@veewo/claw-core@0.1.40` 与 `@veewo/claw@0.1.40` 已成功发布，`npm view @veewo/claw-core version --registry=https://registry.npmjs.org` 和 `npm view @veewo/claw version --registry=https://registry.npmjs.org` 都返回 `0.1.40`。
- repo 支持的本机 CLI 刷新路径是 `npm run install:local-cli`；如果刚发布完就刷新，允许因 npm registry 传播延迟重试一次；最终这条路径已成功把全局 CLI 刷新到 `@veewo/claw@0.1.40`。
- 这轮安装后的稳定验证组是：`claw --version = 0.1.40`、`npm list -g @veewo/claw --depth=0 = @veewo/claw@0.1.40`、`(Get-Command claw).Source = C:\Users\chany\AppData\Roaming\npm\claw.ps1`。
- `npm publish` 里的 `bin[claw]` 归一化警告不能单独当最终结论；发布后仍要回看 `npm view @veewo/claw bin --json` 和真实 `claw` 烟测，确认 registry/runtime 一致。
- 本地 Codex plugin cache 已同步到 `C:\Users\chany\.codex\plugins\cache\claw-kit-local\claw-kit\0.1.40+codex.20260616130425`；当前稳定刷新范围包括 `.codex-plugin/`、`hooks/`、`references/`、`scripts/`、`skills/` 与 `package.json`，并且逐文件 SHA256 已与仓库副本匹配，当前校验结果是 24/24 个同步文件一致。
- canonical `.claw/truth/` markdown 需要保持 UTF-8 BOM；truth ingestion 和 `npm run check` 的 truth encoding audit 都把这当作稳定约束，缺少 BOM 的 truth 文档在 Windows PowerShell 路径上不算完成态。
- `packages/core/src/memory.ts` 保持严格契约：project memory refresh 如果 embedding 生成失败就必须失败，不能降级为 text-only indexing；project search 继续保持 vector-required 契约，缺少 refreshed vector index 时返回 `MEMORY_VECTOR_INDEX_REQUIRED`。
- SessionStart prompt 已从 `packages/cli/src/cli.ts` 硬编码迁移到 guidance config 的 `sessionStart` 字段；`GuidanceConfig` 类型新增 optional `sessionStart` 字段（含 `default.lines` + `recovered.header/snapshotHeader/snapshotFields/planContentHeader`），向后兼容，config 缺失 sessionStart 时 fallback 常量与原硬编码文案逐字一致。
- core 新增 `buildSessionStartDefaultPrompt` 和 `buildSessionStartRecoveredPrompt` 两个 export 函数（`packages/core/src/workflow-guidance.ts`），封装"从 config 读模板 + `renderTemplateString` 变量替换 + 条件字段渲染"；recovered 分支 4 个必显字段无条件渲染，6 个条件字段（nextSteps/recommendedCommands/notes/delegateSubagents/askUser/goalMode）只在有值时才输出。
- 两个 config 文件（`core/src/workflow-guidance.config.json` Codex 默认 + `opencode-adapter/workflow-guidance.opencode.json` OpenCode 变体）已按平台语义差异化 sessionStart 文案：Codex 用 `@claw-kit` / `claw-kit:using-claw-kit`，opencode 用裸 `using-claw-kit` skill 与 "the claw-kit plugin" 措辞，两边 prompt 文本都不含 `plugin://` 链接；core fallback 常量统一为 `@claw-kit`。
- `summarizeRecoveredPlanContent` 函数留在 `cli.ts` 中（纯数据格式化，不属于 prompt 文案），输出作为 `planContentLines: string[]` 参数传入 core 的 recovered prompt builder。
- `codex-adapter/hooks/session-start-recovery.mjs` 是已删除的废弃并行实现（文案曾与 canonical CLI 版本不一致且无 live hook 绑定）；该文件与关联 truth 文档引用现已清理，canonical SessionStart 入口为 `claw hook SessionStart` CLI 命令。
- SessionStart prompt 配置化与 plugin 委托架构决策见 ADR `session-start-prompt-config-delegation`。
- 平台 prompt 语义隔离的稳定事实：`plugin://claw-kit@claw-kit-local` 是 Codex 专用 markdown 插件链接（Codex 解析 `[@claw-kit](plugin://...)`），opencode 完全不解析 `plugin://` 协议，而是通过 `skill` 工具以裸 skill 名（如 `using-claw-kit`）加载、经 opencode `skills.paths` 注册；因此 Codex sessionStart 用 `@claw-kit` / `claw-kit:using-claw-kit`，opencode sessionStart 用裸 skill 名且不含 `plugin://`，core fallback 常量统一为 `@claw-kit`。详见 `.claw/truth/features/platform-sessionstart-prompt-isolation.md`。ADR `session-start-prompt-config-delegation` 第 4 节原把这套平台归属写反了，代码/配置已按正确归属落地，ADR 第 4 节已修正并附修正记录。
- OpenCode subplan 支持来自本地 claw-kit shared core，而不是 upstream opencode：`claw subplan create` 调用 `createSubplan`，core 在 `plans/` 下写 child plan 并记录 `parentPlan` / `parentTaskId`，completion hook 通过 `subplanClosureCandidate` 触发 `completeSubplanAndResumeParent` 恢复 parent plan；`packages/opencode-adapter/plugin/index.ts` 只负责注入 `CLAW_HOST=opencode` 与 `workflow-guidance.opencode.json`，让 OpenCode shell 调用同一套 CLI/core 生命周期。详见 `.claw/truth/features/opencode-subplan-support.md`；canonical ADR 为 `opencode-subplan-uses-shared-core-lifecycle`。
- opencode subagent agent 定义（truth-writer、adr-writer）不应硬编码 `model:` 字段，应继承主 agent model；canonical ADR 为 `opencode-subagent-agent-model-inheritance`。
- claw-kit 维护 Codex 和 OpenCode 两条对称的插件安装链路（`*-plugin-bundle.mjs` → `install-*-plugin.mjs` → `install-*-plugin.ps1`），重叠行为（`shouldCopyEntry` 过滤、`Assert-Command` node 预检、`--source-dir` 透传）必须对称，差异只在 host 专属能力。详见 `.claw/truth/features/opencode-plugin-install-pipeline.md`。
- OpenCode 安装目标为 `~/.config/opencode/plugins/claw-kit/`（固定目录，无版本化），需创建 `claw-kit.ts` shim、向 `opencode.json` 幂等注入 `skills.paths`、拷贝 agent 定义到 `agent/` 目录；安装后需重启 OpenCode。Codex 用版本化目录且不需要 shim / config 注入 / agent 拷贝。
- 两个插件的测试套件（`test:codex-plugin` 3 测试 + `test:opencode-plugin` 4 测试）均用 `node:test` 内置 runner + 临时目录隔离，零外部依赖。
- `claw direct` 仍然是低复杂度 round 的隐藏兼容入口，不应被宣传成与 `plan create` 并列的公开 workflow concept；其合同继续保持轻量，并只在确有复用价值时派发 `truth-writer`。
- `tsc` 不会自动清理已移除模板路径留下的旧输出，因此 `dist` ghost output 必须在 bundle / distribution / closeout 检查里显式判失败，不能只看编译成功。
- 评估期间生成的 test skills / templates 属于一次性验证产物，完成评估后应从仓库中清理，不应进入 shared-skill sync 默认名单或长期可见的 skill surface。
- `docs/technical-principles.html` 的技术原理插图已收敛为 6 组并排对照图；最终图必须用独立 `imagegen` 资产替换内联 `svg.mini-diagram` 占位，并用浏览器实际预览确认新资产生效，而不是依赖 `file://` 旧缓存。
- 从 `0.1.62` 起，npm 发布必须使用 `npm run verify:release` / `npm run publish:release` 的 GitHub-source gate。仓库所有者默认直接在 `main` 交付；仅在明确要求 review 时才创建分支或 PR。发布前必须满足 `HEAD == origin/main` 和干净工作区，npm publish 后也必须仍然干净；有用改动先提交并推送，不能用 stash 绕过门禁。实际生成的 Codex bundle 仍须包含 `planning`、`config`、`update`、`create-claw-skill` 及所需模板；release commit `472635e` 已推送并标记为 `v0.1.62`，两个 npm 包均已发布到 `0.1.62`。
- `v0.1.62` GitHub Release 已发布 `claw-kit-codex-plugin-0.1.62.zip`，用于跨电脑安装同一版本的 Codex 插件；安装验收是本地 cache 发现 10 个 skills，且包含 `planning`、`config`、`update`、`create-claw-skill`。
- Plan template 现在采用统一架构：`claw plan create` 与 `claw subplan create` 都通过 `writePlan(...) -> createPlanFromTemplate(...) -> resolvePlanTemplate(...)` 共用模板发现、schema 判别和实例化；built-in / project legacy `SeedPlanTemplate`、显式 `CLAW_SKILL_ROOTS`、项目或源码 checkout 的 `shared/skills`、Codex / OpenCode host skill roots 都是同一 resolver 的候选来源。subplan 只在统一实例化后补 `parentPlan` / `parentTaskId` 和父任务 execution linkage。full template 的 `configOverride`、task `guidance.onDone`、`choiceId` 已进入运行时，CLI compact plan response 保留 `workflowGuidance.summary`。最终验证通过全仓 `npm run check`、Core 90/90、CLI 56/56、Codex bundle 6/6、OpenCode bundle 5/5；隔离 live smoke 证明 root/subplan 均由 canonical `shared/skills/update/TEMPLATE.json` 生成 `process.active`、3 tasks，child linkage 正确，`choice-id codex` 返回预期 summary 与 `nextTask = 2`。详见 `features/shared-planning-skill-source.md`。
- Writer 的 canonical 主从边界已落地：Codex `truth-writer` / `adr-writer` `SKILL.md` 均为 12 行 delegated-subagent-only 二级路由，禁止 main agent 激活，只接受窄输入并要求 writer 完整读取唯一对应 TRUTH/ADR reference，完整写作规范已迁入 references 且不得转述回主 agent。`packages/core/src/workflow-guidance.config.json` 允许返回 truth delegate contract，但只有 main agent 判断 completed work 含 reusable truth 时才执行；ADR 仍是 root-plan closeout required，既有结构化 delegate 参数保持不变。落地验证通过 writer 定向 `1/1`、core `89/89`、CLI `55/55`、两端 plugin bundle `5/5` 及 `npm run check`；adapter 全量测试剩余的 researcher search-inline 旧断言是独立测试漂移，不是 writer 回归证据。
- `0.1.63` 将 Codex 官方 Git marketplace 路径固化为 `.agents/plugins/marketplace.json -> ./packages/codex-adapter`：该源树在 Git 中已物化 `planning`、`config`、`update`、`create-claw-skill` 及全部资源，`verifySharedSkillsSynced(...)` / `assertSharedSkillsSynced(...)` 让缺失或漂移在 release gate 中失败，bundle 导出不再隐式同步掩盖问题，marketplace-style cache copy 测试验证四个共享 skills 与模板/helper 完整。用户规范安装/升级入口是 `codex plugin marketplace add chanyuenpang/claw-kit --ref main` / `codex plugin marketplace upgrade claw-kit`，直接 cache 安装仅供维护者开发。模板侧 `plan create`、`subplan create`、`template validate` 已统一复用 `resolveSeedPlanTemplate(...)`，validate 额外返回 `choiceRequiredTasks`。详见 `features/shared-planning-skill-source.md`。
- `0.1.63` 已通过 `npm run publish:release` 正式发布：`@veewo/claw-core` 与 `@veewo/claw` 的 npm `latest` 均为 `0.1.63`；npm 将 CLI bin 从 `./dist/bin.js` 规范化为 `dist/bin.js`，但 registry 仍保留 `bin.claw`，真实 `npm install -g @veewo/claw@0.1.63` 后 `claw --version = 0.1.63`。本机开发 cache 与 marketplace cache 分别位于 `...\plugins\cache\claw-kit-local\claw-kit\0.1.63+codex.20260715132514` 和 `...\plugins\cache\claw-kit\claw-kit\0.1.63+codex.20260715132514`；marketplace cache 已实际验证四个共享 skills、`update` / `create-claw-skill` 模板、`choiceRequiredTasks` 与 `create-claw-skill-stub.mjs` 完整可用。GitHub Release `v0.1.63` 已创建并上传 `claw-kit-codex-plugin-0.1.63.zip`（`45086` bytes，SHA256 `cba118e3e7f60c5a930afa9e604c367bbc1e749fb43d2ec9e90fedcd4697a7f5`）。发布 closeout 时 `main...origin/main = 0/0` 且工作树干净。详见 `features/published-npm-packages.md`。
- `0.1.64` release closeout 已完成：source commits 为 `753f92a`、`c4cd18a`、`bfbff96`，`origin/main` 已对齐；registry `latest` 已指向双包 `0.1.64`，全局 `claw --version = 0.1.64` 且 `claw context` 的 CLI/project version 与 protocol check 一致。Codex development marketplace source 与 `0.1.64+codex.20260716013733` cache 的 manifest 一致，排除测试文件后 `29` 个 payload hash 全匹配；installed guidance 验证 conditional truth writer 与 required ADR writer 合同。GitHub Release `v0.1.64` 已上传 `claw-kit-codex-plugin-0.1.64.zip`（`46421` bytes，SHA256 `a52bdde9dc8962e527a04ddc18ec0df8404c9ac0adf92d9ccf34034632301c83`）。详见 `features/published-npm-packages.md`。
- 正式 claw workflow 的端到端成本与真实路径见 `features/workflow-cost-and-runtime-path.md`：审计覆盖 thread-bound skill snapshot 合同漂移、plan lifecycle/meta-task mutation、`per_task` truth 条件语义、query embedding、completion refresh/SQLite/GitNexus 成本，并给出 P0-P3 优化顺序与可回归指标。
- canonical ADR: `workflow-cost-optimization-route.md`；该 ADR 接受分阶段优化方向与 A/B 门槛，不表示产品优化已经实现。
- Codex 开发安装的 active surface 由 marketplace identity/source 决定，不能用 cache 最高版本目录代替；`scripts/install-codex-plugin.*` 现在先校验并刷新 local marketplace source，再从该 source 写 versioned cache，且安装后需重启 Codex、创建新任务。详见 `features/shared-planning-skill-source.md`。
- Codex update 的完成边界是 active identity/source，不是 cache 最高版本：官方 identity 为 `claw-kit@claw-kit`，marketplace upgrade 后需重新安装/启用并处理 stale same-name identity；验收必须对齐 active identity、source manifest、cache manifest、target version，并在 restart/new task 后确认 loaded locator。详见 `features/shared-planning-skill-source.md`。
- Skill instructions 默认采用正向操作合同：正文先建立角色、输入、顺序、选择条件和完成标准，反向限制只保留安全、数据保护、授权、职责或协议所需的短边界；既有 skill wording 优先做高频入口和 writer 的小幅定点修正，并保持 Codex、shared 与 OpenCode 行为一致。canonical ADR: `skill-instructions-prefer-positive-operational-contracts.md`。
